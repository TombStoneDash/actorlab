import Stripe from "stripe"; export default async function h(req,res){ if(req.method!=="POST") return res.status(405).end(); const s=new Stripe(process.env.STRIPE_SECRET_KEY!,{apiVersion:"2024-04-10"}); const email=req.body?.email||"pro.actor@example.com"; const portal=await s.billingPortal.sessions.create({customer_email:email, return_url:`${process.env.NEXT_PUBLIC_BASE_URL}/account`}); res.redirect(303, portal.url); }
